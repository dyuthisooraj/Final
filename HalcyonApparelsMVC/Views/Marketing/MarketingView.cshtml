@model HalcyonApparelsMVC.DTO.MapDTO
@{
    ViewData["Title"] = "Marketing";
    var aclist = ViewBag.acclist as List<string>;
}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">



<h3 style="color: black; margin-top:15px; margin-bottom:35px; font-family: 'Roboto', sans-serif;">Marketing</h3>

<div class="wrap">
    <form asp-action="MarketingView" method="post">
    <select name="accessorytype" asp-for="accessorytype" asp-items="new SelectList(ViewBag.acclist)">
     
    </select>
        <select name="productType" asp-for="productType" asp-items="new SelectList(ViewBag.ptypelist)">
    </select>
    <div style="margin-top:25px;">

    <input type="submit" value="Submit" class="btn btn-primary" data-inline="true" />
    </div>

       @* <i class="bi bi bi-file-plus-fill" onclick=" location.href='@Url.Action("AccessoryTypeView", "Marketing")'" style="color:blue;"></i>*@
</form>

    @*<div style="margin-top: 25px;">

        <button type="button" value="Send Mails" class="btn btn-primary"  onclick=" location.href='@Url.Action("Mail", "Marketing")' ">Send Emails</button>
    </div>*@
</div>

